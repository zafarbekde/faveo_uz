import React from 'react'
import './modal.css'

function Modal({close}) {
  return (
    <div>
      dasdasda
      <button onClick={() => close()}> close</button>
    </div>
  )
}

export default Modal
